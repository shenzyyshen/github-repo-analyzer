# GitHub token (repo scope for public repos; adjust as needed)
GITHUB_TOKEN=

# Database URL (used by Prisma)
DATABASE_URL=postgresql://user:password@localhost:5432/repo_metrics

# Server port (default 3005)
PORT=3005
