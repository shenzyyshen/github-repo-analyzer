services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: repo_metrics
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
